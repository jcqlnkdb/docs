---
title: 'Update task'
openapi: 'PATCH /tasks/{task_id}'
---
<Note>

PATCH is a partial update: include only the fields you want to change. Valid `status` values are `pending`, `in_progress`, and `done`.

</Note>
