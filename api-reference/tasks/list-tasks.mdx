---
title: 'List tasks'
openapi: 'GET /projects/{project_id}/tasks'
---
<Note>

If the project exists but has no tasks, the response returns `tasks: []` with status 200 — this is not an error.

</Note>
